# Loop Control

## 目标

限制模型在完成主任务后不断提出并执行额外建议，避免流程失控。

## 建议规则

1. 每轮主任务之外，最多提出 1-2 个增量建议
2. 建议必须分类：
   - `blocking`
   - `important but deferred`
   - `nice to have`
3. 用户未明确批准前，建议不得自动升级为新任务
4. 同一需求连续返工超过阈值后，必须升级到人工决策

