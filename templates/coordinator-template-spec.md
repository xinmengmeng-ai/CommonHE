# Coordinator Template Spec

## 目标

定义 `.codex/COORDINATOR-SUBAGENTS.md` 这类主控调度文件的通用模板要求。

## 最低内容

1. 主控定位
2. 默认真源入口
3. 角色与手册映射
4. 调度触发规则
5. 验收与门禁
6. 变更检测策略
7. 交接清单

## 模板要求

- 路径按目标项目真实结构生成
- 允许角色按项目方案增减
- 不默认绑定某个项目专有角色

