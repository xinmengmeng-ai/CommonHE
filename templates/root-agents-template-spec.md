# Root AGENTS Template Spec

## 目标

定义通用项目根目录 `AGENTS.md` 模板应覆盖哪些核心内容。

## 最低要求

1. 主控定位
2. 协作边界
3. 真源文档入口
4. 执行代理分工规则
5. 验收门禁
6. 输出协议
7. 归档规则

## 模板使用要求

- 模板内容必须允许按项目类型替换
- 模板内路径必须按目标项目根目录真实结构组织
- 复制到新项目后不应再引用 `CommonHE` 自身路径
